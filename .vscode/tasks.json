{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Deploy site to GitHub",
      "type": "shell",
      "command": "bash -c 'read -p \"Push changes to GitHub? (y/n) \" ans; if [[ $ans == y ]]; then ./deploy.sh; else echo \"Push cancelled.\"; fi'",
      "problemMatcher": []
    }
  ]
}
